make file ; file containt commands with variable name and target to create object (file  .o)
and remove them.

Makefile is a build automation tool that allows developers to 
define a set of rules and dependencies for compiling their code.

target ; The target in a makefile rule is usually the name of a file that is part of 
the project.

rule ; 